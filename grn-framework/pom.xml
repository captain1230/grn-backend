<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.sssys.grn</groupId>
        <artifactId>grn-backend</artifactId>
        <version>1.0.0</version>
    </parent>

    <!-- 子模块自身的artifactId（不能和根模块重复） -->
    <artifactId>grn-framework</artifactId>
    <!-- 子模块打包方式：非父模块必须是jar（根模块才是pom） -->
    <packaging>jar</packaging>

    <name>grn-framework</name>
    <description>grn框架核心模块</description>

    <dependencies>
        <!-- ========== Redis核心依赖 ========== -->
        <!-- Spring Data Redis（含Lettuce客户端，Spring Boot 3默认） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <!-- Redisson核心依赖（适配Spring Boot 3/Jakarta EE） -->
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson-spring-boot-starter</artifactId>
        </dependency>
        <!-- Hutool工具包（你的配置类用到了ReflectUtil） -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>
        <!-- Spring Boot基础依赖 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <!-- Jackson核心依赖 -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!-- Jackson注解支持（增强序列化能力） -->
        <dependency>
            <groupId>com.fasterxml.jackson.module</groupId>
            <artifactId>jackson-module-parameter-names</artifactId>
        </dependency>
        <!-- Jackson Java 8时间模块（解决LocalDateTime序列化问题） -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
    </dependencies>

</project>
